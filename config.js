// Configuración centralizada
// En Railway, estas variables se configuran en el dashboard
const CONFIG = {
    // Para desarrollo local, puedes cambiar estos valores temporalmente
    // En producción (Railway), se usarán las variables de entorno
    SENDBINDER_API_KEY: 'SENDBINDER_API_KEY_PLACEHOLDER', // Railway reemplazará esto
    SENDBINDER_EMAIL: 'castilla@joaquincastilla.com'
};

// Nota: Este archivo está preparado para Railway
// Configura las variables de entorno en Railway dashboard:
// - SENDBINDER_API_KEY: tu clave API de SendBinder
